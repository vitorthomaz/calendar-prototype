const theme = {
  typography: {},
  colors: {
    pageBackground: '#e0e0e0',
    primary: '#f5f5f5',
    clearBlue: '#2879FF'
  },
  spacing: {}
};

export type CustomTheme = typeof theme;

export default theme;
