import React from 'react';

import { Card } from '../../components';

import { Container } from './styles';

const Menu = () => {
  return (
    <Card>
      <Container>
        <p>// Action Buttons</p>
        <p>// Today Date</p>
        <p>// Reminder List</p>
      </Container>
    </Card>
  );
};

export default Menu;
